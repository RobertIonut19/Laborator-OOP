#ifndef globalFunctions
#pragma once
#include "Student.h"

class globalFunctions {
public:
	void compareName(student s1, student s2);
	void compareMath(student s1, student s2);
	void compareEng(student s1, student s2);
	void compareHis(student s1, student s2);
	void compareAvg(student s1, student s2);
};
#endif